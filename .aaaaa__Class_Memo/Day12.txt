클라이언트와 서버가 잇고, 이 둘이 데이터를 주고받는다 . 

자바에서는 이런 데이터를 주고받는것을 , 스트림의 형태로 주고받는다.

스트림(Stream)은? 한방향으로만 흐른다.
즉 데이터를 보내는애는 보내기만하고 받는애는 받기만한다.

내보내는 것은 아웃
받는것은 인풋

1번째 방법

FileOutputStream은 파일을 만들어주는 메소드, 경로설정시 C드라이브 바로밑에 폴더까지만 , 접근가능, 윈도우 보안땜시

write는 파일에 적어준다.
getBytes()는 스트링을 바이트배열로 바꾸어준다.
이떄 \r\n 는 엔터를 말한다.

내용을바꾸고 실행 할 경우 ,덮어쓰기가 된다.

그래서 2번째방법을 더 쓴다

2번째 방법

FileWriter 클래스 사용

내부에 이미 Stream이 구현되어있다.

위처럼 그대로 내용을 바꿔서 사용시? 똑같이 덮어쓴다. 덮어쓰는기 기본이기때문

근데
FileWriter fw; 
fw= new FileWriter("경로",true);
를 할경우?

붙여쓰기 가 된다!!
(false는 덮어쓰기)


File 클래스는 

File tempFile = new File("경로");
if(tempFile.exists()==true){

있으면 true, 없으면 false


}






buffer란 , 미리 담아두는 공간이라고 보면된다. 버퍼에는 미리 싹 담아놓고
플레이를 시킬경우 버퍼에서 필요한것만 가져오는거지

readLine() == null  이라는건 , 내가 파일을 싹다 읽어왔는지를 확인하는 조건


